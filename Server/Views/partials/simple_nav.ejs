<!-- Simple navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/home"><img src="/assets/images/logo-transparent.png" alt="kenny f development logo"></a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
            <%# if/else displays which page is active in the nav %>
            <% if (title === "Home") { %>
              <a class="nav-link active" aria-current="page" href="/home">
              <% } else { %>
                <a class="nav-link" aria-current="page" href="/home">
              <% } %>
              <i class="fas fa-home"></i> Home</a
              >
            </li>
            <li class="nav-item">
              <% if (title === "Register") { %>
                <a class="nav-link active" aria-current="page" href="/register">
                <% } else { %>
                  <a class="nav-link" aria-current="page" href="/register">
                <% } %><i class="fas fa-clipboard"></i> Register</a
              >
            </li>
              <li class="nav-item">
              <% if (title === "Login") { %>
                <a class="nav-link active" aria-current="page" href="/login">
                <% } else { %>
                  <a class="nav-link" aria-current="page" href="/login">
                <% } %><i class="fas fa-sign-in-alt"></i> Login</a
              >
            </li>

          </ul>
        </div>
      </div>
    </nav>